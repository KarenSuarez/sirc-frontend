import { Routes } from '@angular/router';
import { PanelContadorComponent } from './panel-contador.component';

export const PANEL_CONTADOR_ROUTES: Routes = [
  { path: '', component: PanelContadorComponent }
];
